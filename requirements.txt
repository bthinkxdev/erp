# EcomicX ERP — runtime dependencies
# Python: 3.10, 3.11, 3.12, or 3.13 (required by Django 5.2)
#
# Install:  python -m pip install -r requirements.txt
# Upgrade:  python -m pip install -U -r requirements.txt

# --- Application ---
Django==5.2.12
Pillow==12.2.0

# --- Django stack (pinned for reproducible installs) ---
asgiref==3.11.1
sqlparse==0.5.5
tzdata==2025.3

# --- Production ---
gunicorn>=23.0,<24
psycopg[binary]>=3.2,<4
